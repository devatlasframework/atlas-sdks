# atlas-sdk (TypeScript)

TypeScript SDK for the ATLAS /v1 API. Planned: TS strict, ESM.

**No code yet.** It will be generated from the published contract (`atlas-docs/api/openapi.yaml`)
and pinned to that contract's `info.version`.
